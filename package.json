{
  "name": "division2",
  "homepage": "https://://cdn.jsdelivr.net/gh/kjy9310/division2@gh-pages/",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.5",
    "react-dom": "^16.8.5",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "react-scripts build && cd build && git init && git remote add origin https://github.com/kjy9310/division2.git && git checkout -b gh-pages && git add . && git commit -m 'deploy' && git push --force --set-upstream origin gh-pages"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
